[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"] # Depends(...) en valeur par défaut est l'idiome standard FastAPI

[tool.pytest.ini_options]
asyncio_mode = "auto"
